# Project Memory

Running log of decisions, deviations, and important context for future sessions.

---

## Session 1 — 2026-05-19

### Stack deviations from the handoff (intentional, locked in)

| Area | Handoff said | What we actually did | Why |
|---|---|---|---|
| shadcn CLI | Radix UI primitives | `@base-ui/react` (shadcn v4.7.0) | Latest CLI changed underlying primitive library; API is identical |
| Fonts | `next/font/google` with `Geist` | `geist` npm package (`GeistSans`, `GeistMono`) | `Geist` not available in `next/font/google` on Next.js 14 |
| react-day-picker | v8/v9 assumed | v10 installed | `table` className key renamed to `month_grid` in v10; fixed in calendar.tsx |
| Auth middleware | Single `lib/auth.ts` for both | Split: `auth.config.ts` (edge-safe) + `lib/auth.ts` (Node-only) | bcryptjs is not Edge Runtime compatible; middleware must use edge-safe config only |
| `globals.css` | Clean design tokens | shadcn init removed `--background`/`--foreground` from unlayered `:root` | Fixed by re-adding them; unlayered `:root` beats shadcn's `@layer base` oklch values via CSS cascade |
| `form.tsx` | Generated by shadcn CLI | Hand-written react-hook-form wrapper | Not in new shadcn registry; standard implementation |

### Key files

| File | Purpose |
|---|---|
| `auth.config.ts` | Edge-safe NextAuth config (no bcryptjs, no Prisma) — used by middleware |
| `lib/auth.ts` | Full NextAuth config with Credentials provider and bcryptjs — used by server actions and API routes |
| `lib/prisma.ts` | Singleton Prisma client (real implementation — user updated from stub) |
| `app/globals.css` | Full HSL design token system; unlayered `:root` and `.dark` override shadcn's `@layer base` |
| `tailwind.config.ts` | Maps all CSS vars to Tailwind colour utilities including finance semantics (`income`, `expense`, `savings`, `neutral2`) |
| `components/shell/` | AppShell, Sidebar, Header, LogoMark — the 3-island layout |

### State at end of session

- `pnpm dev` starts, middleware live (`307 → /login` for unauthenticated requests)
- `pnpm build` passes clean — 11 routes, 0 type errors
- `lib/prisma.ts` is now a real singleton client (user updated it); needs a running Postgres + `prisma migrate dev` to actually work
- Auth won't complete a login until Session 2 seeds the database
- `upcomingRenewalsCount` hardcoded to `6` in `AppShell` — replace in Session 4
- Commit: `58013fa feat: bootstrap project, tokens, shell, auth scaffold`

---
